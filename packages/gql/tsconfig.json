{
  "extends": "../../tsconfig.json",
  // TODO not sure why this is needed even though proper paths is declared
  //  in ../../tsconfig.build.json
  "compilerOptions": {
    "composite": true,
    "baseUrl": ".",
    "paths": {
      "@nftcom/gql/*": [
        "src/*"
      ]
    }
  },
  "references": [
    {
      "path": "../shared/tsconfig.json"
    }
  ]
}
