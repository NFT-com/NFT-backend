{
    "name": "@nftcom/redis-watcher",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "build": "npm run clean && npm run compile",
        "postbuild": "cp package.json dist/package.json && cd dist && npm install --only=production",
        "clean": "rimraf dist tsconfig.tsbuildinfo",
        "compile": "tsc && tsc-alias",
        "test": "jest"
    },
    "dependencies": {
        "bull": "^4.2.0",
        "tslib": "^2.4.0"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.97",
        "@types/bull": "^3.15.7",
        "@types/jest": "^27.5.1",
        "@types/node": "^17.0.35",
        "jest": "^28.1.0",
        "rimraf": "^3.0.2",
        "ts-jest": "^28.0.3",
        "tsc-alias": "^1.6.7",
        "typescript": "^4.6.4"
    }
}
