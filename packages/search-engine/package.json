{
  "name": "@nftcom/search-engine",
  "version": "0.0.1",
  "main": "dist/packages/search-engine/src/index",
  "types": "dist/packages/search-engine/src/index",
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rimraf dist tsconfig.lib.tsbuildinfo",
    "compile": "tsc -p tsconfig.lib.json && tsc-alias -p tsconfig.lib.json"
  }
}
