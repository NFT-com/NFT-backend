type FileUploadOutput {
  bucket: String!
  accessKey: String!
  secretKey: String!
  sessionToken: String!
}

extend type Query {
  fetchEthUsd: String!
}

extend type Mutation {
  "AUTHENTICATED"
  uploadFileSession: FileUploadOutput!
}
