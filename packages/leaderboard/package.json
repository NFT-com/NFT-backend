{
  "name": "@nftcom/leaderboard",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index",
  "source": "./src/index.ts",
  "types": "./src/index",
  "scripts": {
    "build": "tsc -p tsconfig.lib.json",
    "postbuild": "./scripts/postbuild.sh",
    "clean": "rm -rf dist",
    "dev": "tsc -p tsconfig.lib.json -w",
    "lint": "eslint \"src/**/*.ts*\"",
    "test": "jest --detectOpenHandles"
  },
  "dependencies": {
    "@nftcom/cache": "workspace:*",
    "@nftcom/nftport-client": "workspace:*",
    "@nftcom/shared": "workspace:*"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "tsup": "^6.7.0",
    "typescript": "^5.0.4"
  }
}
