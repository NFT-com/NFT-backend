type Collection {
    id: ID!
    contract: Address!
    name: String
}

input CollectionInput {
    contract: Address
    network: String
}

extend type Query {
  collection(input: CollectionInput!): Collection
}
