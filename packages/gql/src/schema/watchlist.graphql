type Watchlist {
    nftItems: [NFT!]
    profileItems: [Profile!]
    collectionItems: [Collection!]
}

extend type Query {
    watchlist: Watchlist!
}

extend type Mutation {
    addToWatchlist(itemId: ID!): Watchlist!
    deleteFromWatchlist(itemId: ID!): Watchlist!
}
