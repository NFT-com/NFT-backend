{
  "name": "@nftcom/sales-processor",
  "version": "0.0.1",
  "description": "Keep sales data up to date in NFT.com database",
  "main": "dist/packages/sales-processor/src/index",
  "types": "dist/packages/sales-processor/src/index",
  "devDependencies": {
    "rimraf": "^3.0.2",
    "tsc-alias": "^1.8.5",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rimraf dist tsconfig.tsbuildinfo",
    "compile": "tsc && tsc-alias",
    "start": "node ./dist/packages/sales-processor/src/index"
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
