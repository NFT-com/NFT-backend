type FileUploadOutput {
  bucket: String!
  accessKey: String!
  secretKey: String!
  sessionToken: String!
}

extend type Query {
  fetchEthUsd: Float!
}

extend type Mutation {
  "AUTHENTICATED"
  uploadFileSession: FileUploadOutput!
}
