type Approval {
  id: ID!
  amount: Uint256!
  deadline: String!
  nonce: Int!
  signature: Signature!
  txHash: String!
#  user: User
  wallet: Wallet
  createdAt: DateTime!
}

input ApprovalInput {
  amount: Uint256!
  currency: Address!
  deadline: String!
  nonce: Int!
  signature: SignatureInput!
  txHash: String!
  wallet: WalletInput!
}

#extend type Query {
#
#}

extend type Mutation {
  approveAmount(input: ApprovalInput!): Approval!
}
