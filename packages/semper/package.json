{
  "name": "@nftcom/semper",
  "version": "1.0.0",
  "description": "Search engine utilities",
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rimraf dist tsconfig.tsbuildinfo",
    "compile": "tsc && tsc-alias",
    "dev": "doppler run --preserve-env -- node dist/index.js",
    "start": "npm run dev"
  },
  "dependencies": {
    "@babel/runtime": "^7.18.3",
    "typesense": "^1.3.0"
  }
}
